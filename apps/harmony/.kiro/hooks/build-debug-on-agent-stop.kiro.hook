{
  "enabled": true,
  "name": "Build Debug After Agent Stop",
  "description": "每次 agent 完成所有代码修改后，自动运行 pnpm run build:debug 验证构建是否通过",
  "version": "1",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "runCommand",
    "command": "pnpm run build:debug",
    "timeout": 120
  }
}